package com.bikininjas.corelib.objective;

/**
 * Enumeration of objective types matching the sealed interface implementations.
 */
public enum ObjectiveType {
    KILL,
    COLLECT,
    REACH,
    SURVIVE
}
